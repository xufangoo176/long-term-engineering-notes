# 哪些字段需要建立索引

1、平常我们在工作中接到一个需求，然后需要针对这个需求写 crud 嘛。
此时，我们可以去看原型图，找一找页面上的所有查询条件，它们所对应的数据库的字段，基本都是要建立索引的。
2、每张表的主键 ID，也是需要建立索引，但这个基本不用操心。创建表的时候，数据库会自动创建索引。
3、JOIN 的关联字段，也需要建立。否则会触发全表扫描。
4、order by, group by 的字段，也需要建立。
5、业务逻辑上，如果某个字段要求是唯一值，那么得创建唯一索引。